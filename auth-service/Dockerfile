FROM node:20-alpine

RUN mkdir -p /opt/app
ADD . /opt/app
WORKDIR  /opt/app

ENV NODE_ENV=$NODE_ENV
ENV AUTH_SERVICE_SECRET=$AUTH_SERVICE_SECRET
ENV DOTENV_CONFIG_PATH=$DOTENV_CONFIG_PATH

EXPOSE 9090
